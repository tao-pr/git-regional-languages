package gitdatasource

/**
 * JSON datasource handler object
 */
object JSONDatasource{

  private def readJSON(path: String) = String {

  }

  /**
   * Load the entire bulk of the language distributions
   * @param {String} path to {dist.json}
   * @return {List[LanguageCodeDistributions]} list of all spatial code distribution by each language
   */
  def loadDistributions(path: String) = List[LanguageCodeDistributions] {
    // TAOTODO:

  }  
}